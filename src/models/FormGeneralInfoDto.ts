export interface FormGeneralInfoDto {
    /**
     * 0 não muda versão
     * 1 - adiciona 1 dígito ao final da versão
     * 2 - adiciona 1 dígito ao início da versão
     */
    versionOption: string;
}
